import type { Event } from "@/data/events";
import { Calendar, Clock, Music } from "lucide-react";
export function EventCard({ event }: { event: Event }) {return <article className="group overflow-hidden rounded-[2rem] border border-white/10 bg-white/[.06] shadow-card backdrop-blur-xl"><div className="h-56 bg-cover bg-center transition duration-700 group-hover:scale-105" style={{backgroundImage:`linear-gradient(to top,rgba(0,0,0,.78),transparent),url(${event.image})`}}/><div className="p-6"><span className="rounded-full bg-fuchsia-500/15 px-3 py-1 text-xs font-bold text-fuchsia-200">{event.category}</span><h3 className="mt-4 text-2xl font-black text-white">{event.name}</h3><div className="mt-4 grid gap-2 text-sm text-white/65"><p><Calendar size={15} className="mr-2 inline"/>{event.date}</p><p><Clock size={15} className="mr-2 inline"/>{event.time}</p><p><Music size={15} className="mr-2 inline"/>{event.dj} · {event.genre}</p></div><a href="/vip" className="mt-6 inline-flex rounded-full bg-white px-5 py-3 text-sm font-black text-black transition hover:bg-cyan-200">{event.cta}</a></div></article>}
