"use client";
import { useState } from "react";
import { galleryImages } from "@/data/gallery";
export function GalleryGrid(){const [active,setActive]=useState<string|null>(null);return <><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{galleryImages.map((img,i)=><button onClick={()=>setActive(img.src)} key={img.src} className={`group relative h-72 overflow-hidden rounded-[2rem] ${i===0?'lg:col-span-2':''}`}><div className="absolute inset-0 bg-cover bg-center transition duration-700 group-hover:scale-110" style={{backgroundImage:`url(${img.src})`}}/><div className="absolute inset-0 bg-gradient-to-t from-black/85 to-transparent"/><div className="absolute bottom-5 left-5 text-left"><p className="text-sm font-bold text-cyan-200">{img.category}</p><h3 className="text-2xl font-black text-white">{img.title}</h3></div></button>)}</div>{active&&<button onClick={()=>setActive(null)} className="fixed inset-0 z-[80] grid place-items-center bg-black/90 p-4"><img alt="Club preview" src={active} className="max-h-[84vh] rounded-[2rem] object-contain shadow-neon"/></button>}</>}
