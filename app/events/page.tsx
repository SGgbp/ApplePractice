import { AnimatedSection } from "@/components/AnimatedSection";
import { EventCard } from "@/components/EventCard";
import { eventFilters, events } from "@/data/events";
export default function EventsPage(){return <main className="px-4 pb-20 pt-28 text-white sm:px-6 lg:px-8"><AnimatedSection className="mx-auto max-w-7xl"><p className="font-bold uppercase tracking-[.35em] text-cyan-200">Events calendar</p><h1 className="mt-4 text-5xl font-black sm:text-7xl">Upcoming events</h1><p className="mt-5 max-w-3xl text-white/65">Dummy data is isolated in the data folder so Eventbrite, a free events API, or a custom CMS can replace it later without changing the card UI.</p><div className="mt-8 flex flex-wrap gap-3">{eventFilters.map(f=><button key={f} className="rounded-full border border-white/10 bg-white/10 px-5 py-2 text-sm font-bold text-white/75 hover:border-fuchsia-300">{f}</button>)}</div><div className="mt-10 grid gap-6 md:grid-cols-3">{events.map(e=><EventCard key={e.id} event={e}/>)}</div></AnimatedSection></main>}
